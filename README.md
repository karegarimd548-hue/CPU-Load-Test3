# CPU Load Test 3

برنامه اندرویدی تست فشار ۱۰ ثانیه‌ای CPU و RAM.

RAM عمداً با سقف محدود برنامه مدیریت می‌شود تا احتمال کرش سیستم کم شود. پس از ۱۰ ثانیه تست خودکار متوقف و حافظه آزاد می‌شود.

## ساخت APK

Workflow داخل `.github/workflows/build-apk.yml` قرار دارد و در GitHub Actions فایل APK را می‌سازد.
